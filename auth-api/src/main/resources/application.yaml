spring:
  application:
    name: auth-api

  datasource:
    url: jdbc:postgresql://localhost:5434/winwin
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true